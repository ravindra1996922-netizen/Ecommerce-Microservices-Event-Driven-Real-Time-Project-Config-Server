spring:
  datasource:
    url: jdbc:mysql://localhost:3306/realtimeproject
    username: root
    password: qwerty
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

aws:
  access-key-id: YOUR_ACCESS_KEY
  secret-key: YOUR_SECRET_KEY
  region: us-east-1

  s3:
    bucket-name: myapp-product-images1996
